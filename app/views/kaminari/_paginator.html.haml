= paginator.render do
  %nav.text-xs-center
    %ul.pagination.pagination-sm
      = prev_page_tag
      - each_page do |page|
        - if page.left_outer? || page.right_outer? || page.inside_window?
          = page_tag page
      = next_page_tag
